@model IEnumerable<HotelSystemIndustry.Models.Events.EventReservation>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-controller="EventManagement" asp-action="StartMakingReservation">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Status)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EventType)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StartTime)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EndTime)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NumRequiredStaff)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NumGuests)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AgreementDocumentPath)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Status.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EventType.Name)
            </td>
            <td>
                @{ DateTime startTime = item.StartTime.ToLocalTime(); }
                @Html.DisplayFor(modelItem => startTime)
            </td>
            <td>
                @{ DateTime endTime = item.StartTime.ToLocalTime(); }
                @Html.DisplayFor(modelItem => endTime)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NumRequiredStaff)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NumGuests)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AgreementDocumentPath)
            </td>
            <td>
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
