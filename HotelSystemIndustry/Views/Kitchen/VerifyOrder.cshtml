@using HotelSystemIndustry.Models.Kitchen
@using HotelSystemIndustry.ViewModels.Kitchen
@model NewOrderViewModel

@{
    ViewData["Title"] = "Złóż zamówienie – weryfikuj zamówienie";
    @if (User.IsInRole("KitchenEmployee"))
    {
        ViewBag.LowerNavPartial = "_KitchenNavPartial";
    }

    var Products = (IList<KitchenProduct>)ViewBag.Products;
    var OrderTypes = (IList<OrderType>)ViewBag.OrderTypes;
}


<h1>Zweryfikuj złożone zamówienie</h1>
<hr />


<form method="post">
    @Html.AntiForgeryToken()
    <div asp-validation-summary="ModelOnly" class="text-danger" role="alert"></div>

    @Html.HiddenFor(m => m.Type)
    @Html.HiddenFor(m => m.Destination)
    @Html.HiddenFor(m => m.HotelId)

    <dl class="row">
        <dt class="col-sm-2">
            @Html.LabelFor(m => m.HotelId, "Hotel: ")
        </dt>
        <dd class="col-sm-10">
            @ViewBag.HotelName
        </dd>

        <dt class="col-sm-2">
            @Html.LabelFor(m => m.Type, "Typ zamówienia: ")
        </dt>
        <dd class="col-sm-10">
            @OrderTypes.Single(ot => ot.Id == Model.Type).Name
        </dd>

        <dt class="col-sm-2">
            @Html.LabelFor(m => m.Destination, "Miejsce docelowe: ")
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(m => m.Destination)
        </dd>
    </dl>

    @{ decimal totalPrice = 0; }

    <ul>
        @for (var i = 0; i < Model.Products.Count; i++) {
            <li>
                @Html.HiddenFor(m => m.Products[i].ProductId)
                @Html.HiddenFor(m => m.Products[i].Count)
                @{
                    var id = Model.Products[i].ProductId;
                    var count = Model.Products[i].Count;
                    var product = Products.Single(p => p.Id == id);

                    var productPrice = product.Price*count;
                    totalPrice += productPrice;

                    <div class="row">
                        <div class="col-md-8 text-start">@product.Name x @count</div> <div class="col-md-2 text-end">@productPrice.ToString("C2")</div>
                    </div>
                }
            </li>
        }
    </ul>

    <hr />
    <div class="row mx-2 my-4">
        <div class="col-md-7 text-start">Razem: </div> <div class="col-md-3 text-end">@totalPrice.ToString("C2")</div>
    </div>
    
    <input type="submit" value="Prześlij" asp-action="SubmitOrder" class="btn btn-success" />

    <input type="submit" value="Wróć" asp-action="ChooseOrderProducts" class="btn btn-primary" />

    <a asp-action="PlaceOrder" class="btn btn-primary">Anuluj</a>

</form>


@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial.cshtml");
    }
}

