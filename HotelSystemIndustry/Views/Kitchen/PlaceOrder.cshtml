@using HotelSystemIndustry.ViewModels.Kitchen
@model NewOrderViewModel

@{
    ViewData["Title"] = "Złóż zamówienie";
    @if (User.IsInRole("KitchenEmployee"))
    {
        ViewBag.LowerNavPartial = "_KitchenNavPartial";
    }
}


<h1>Złóż zamówienie</h1>
<hr />


<form class="row" method="post" asp-action="ChooseOrderProducts">
    @Html.AntiForgeryToken()
    <div asp-validation-summary="ModelOnly" class="text-danger" role="alert"></div>

    @Html.HiddenFor(m => m.HotelId)
    
    <div class="form-group row my-2">
        <div class="col-md-4">
            @Html.LabelFor(m => m.Type, "Typ zamówienia: ", new {@class = "control-label"})
        </div>
        <div class="col-md-6">
            @Html.DropDownListFor(m => m.Type, (IEnumerable<SelectListItem>)ViewBag.OrderTypes, new {@class = "form-control"})
        </div>
        <span asp-validation-for="Type" class="text-danger"></span>
    </div>

    <div class="form-group row my-2">
        <div class="col-md-4">
            @Html.LabelFor(m => m.Destination, "Miejsce docelowe: ", new {@class = "control-label"})
        </div>
        <div class="col-md-6">
            @Html.TextBoxFor(m => m.Destination, new {@class = "form-control", @placeholder = "Wprowadź numer stolika / numer pokoju / adres docelowy…"})
        </div>
        <span asp-validation-for="Destination" class="text-danger"></span>
    </div>

    <div class="form-group">
        <input type="submit" class="btn btn-primary" value="Wybierz produkty" />
    </div>

</form>


@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial.cshtml");
    }
}

